schema: http://localhost:4000
documents: 'client/**/gql.ts'
generates:
  ./client/gql-types/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
